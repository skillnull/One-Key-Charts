module.exports = {
  tabWidth: 2,
  // 结尾是否添加分号
  semi: false,
  // true: { foo: bar } false: {foo: bar}
  bracketSpacing: true,
  // 箭头函数是否加圆括号 always: (x) => x   avoid: x => x
  arrowParens: "avoid"
}